# SpectrumEye — ML training requirements
# Used by: ml/train.py, ml/evaluate.py, ml/augment.py,
#           ml/split_dataset.py, ml/generate_test_batch.py,
#           ml/prepare_colab_zip.py, ml/notebooks/
#
# Python 3.12 required — TensorFlow does not support 3.13+
#
# Create venv:  cd ml && python3 -m venv .venv
# Activate:     source .venv/bin/activate
# Install:      pip install -r requirements.txt

# Core ML
tensorflow==2.18.0
numpy==1.26.4

# Signal processing
scipy==1.13.1

# Image handling
Pillow==10.4.0

# Visualization
matplotlib==3.9.2
seaborn==0.13.2

# ML utilities
scikit-learn==1.5.2

# Notebook
jupyter==1.1.1
ipykernel==6.29.5

# AWS (used in upload_s3.py — Phase 6)
# boto3==1.35.0

# Pi 5 inference — install separately on ARM after INT8 conversion
# tflite-runtime
