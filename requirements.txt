# SpectrumEye — Edge + Simulation requirements
# Used by: edge/main.py, simulation/simulation_final.py
#
# Python 3.11, 3.12, or 3.13 supported (TF 2.21+ supports 3.13)
#
# Dev (Arch, Python 3.12):
#   python3.12 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# Pi 5 (Debian Trixie, Python 3.13):
#   sudo apt install rtl-sdr
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install --prefer-binary -r requirements.txt

# Core numerics — >=2.0 has Python 3.13 binary wheels (no Fortran compiler needed)
numpy>=2.0.0

# Signal processing — >=1.14 has Python 3.13 binary wheels
scipy>=1.14.0

# Image handling
Pillow>=10.4.0

# CNN inference (classifier.py loads the .keras model)
# TF 2.21+ supports Python 3.13 (Pi 5 / Debian Trixie)
tensorflow>=2.18.0

# WebSocket server (ws_server.py → React dashboard)
websockets>=12.0

# Spectrogram plotting (data_collector.py, rtlsdr_source.py)
matplotlib>=3.9.0

# Optional — Flask display backend (--display flask)
flask>=3.0.0

# Optional — CPU/RAM stats in status heartbeat (main.py)
psutil>=6.0.0

# Optional — AWS cloud pipeline (aws_publisher.py)
# boto3>=1.35.0

# RTL-SDR hardware — exact versions required (tested on Pi 5 / Debian Trixie / Python 3.13)
setuptools==69.5.1
pyrtlsdr==0.2.92
